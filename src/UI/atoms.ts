import { atom } from 'jotai';

export const headerHeightAtom = atom(60);

export const bookmarkListPaddingTopAtom = atom(24);

export const bookmarkItemHeightAtom = atom(44);

export const bookmarkItemMarginBottomAtom = atom(16);
